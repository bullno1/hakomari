#!/bin/runit-sv

SV_SANDBOX="sandbox"

shift
. ${1}

SV_EXEC="exec hako-run \
	--mount $(readlink -f $(which ${SV_BIN})):/bin/${SV_NAME}:ro \
	--mount /lib:/lib:ro \
	${SANDBOX_ARGS} \
	${SV_SANDBOX} -- \
"
SV_BIN=/bin/${SV_NAME}
